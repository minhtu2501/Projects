<h1>Listing carts</h1>

<table>
  <tr>
    <th>Purchased at</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @carts.each do |cart| %>
  <tr>
    <td><%= cart.purchased_at %></td>
    <td><%= link_to 'Show', admin_cart_path(cart) %></td>
    <td><%= link_to 'Edit', edit_admin_cart_path(cart) %></td>
    <td><%= link_to 'Destroy', admin_cart_path(cart), confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Cart', new_admin_cart_path %>